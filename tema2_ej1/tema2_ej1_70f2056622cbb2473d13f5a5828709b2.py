import math


def area_triangulo(base, altura):
    return ((base*altura)/2)


def area_rectangulo(base, altura):
    return (base*altura)


def area_rombo(base, altura):
    return((base*altura)/2)


def area_circulo(radio):
    return (math.pi * math.pow(radio, 2))