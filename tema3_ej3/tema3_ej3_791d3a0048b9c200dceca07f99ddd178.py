# completa el código de la función
class Cuenta:
    def_int_(self,rut,saldo)
        self.rut=rut
        self.saldo=saldo
    def_girar_(self,monto)
        if self.saldo>=monto:
            self.saldo<=monto
            return True
        else:
            return False
 
           