def subst(string,n):
  st_lisy=list(string)
  while len(st_list)>0:
    a=0
    subst=[]
    while a<=n:
      subst=subt+string[a]
      a=a+1
    i=string.count(subst)
    subst_list=[] 
    if i==1:
      subst_list.append(subst)