# por favor escribe aquí tu función
def es_primo(es_primo):
    if not (es_primo/2!=float) and(((es_primo//1)==es_primo) and ((es_primo//es_primo)==1)) or (es_primo%2!=0) or (es_primo==2):
        if (es_primo!=1):
            return(True)
    if es_primo==1:
        return(False)
    else:
        return(False)
