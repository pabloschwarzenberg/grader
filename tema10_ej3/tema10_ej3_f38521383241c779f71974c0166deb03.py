def sopaletras(nombre_archivo, palabras):
    archivo = open(nombre_archivo, "r")
    m = [("C", "A", "S", "A"),
         ("E", "R", "G", "H"),
         ("I", "O", "A", "M")]
    if i in range(4):
        if j in range(4):
    archivo.close()
    return [(palabras[0], [0, 0], "diagonal")]
