# completa el código de la función
def suma_divisores(a):
  lista=[]
  for x in range(1,a):
    if a%x==0:
      lista.append(x)
    sum(lista)
  if sum(lista)==1:
    return sum(lista),True 
  else:
    return sum(lista),False
 



  
           