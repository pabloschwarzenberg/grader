# completa el código de la función
def suma_divisores():
   
  divisores = []
  for i in range(1,n):
    if n % i == 0:
      divisores.append(i)
  suma = 0
  for i in divisores:
    suma = suma + i
    
  if suma==1:
    
  return
