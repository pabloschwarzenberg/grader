# completa el código de la función
def suma_divisores(numeros1):
     inicio = 0
     for i in range(int(numeros1)):
          if i % multiplo == 0:
             inicio += 1
          return inicio
     for element in range(len(numbers1)):
          if element % mul == 0:
             suma += 1
          return suma