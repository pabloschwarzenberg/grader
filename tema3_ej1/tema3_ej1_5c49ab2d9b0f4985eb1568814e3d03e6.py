# completa el código de la función
def suma_divisores(a):
    inicio=1
    z=0
    while inicio<a:
        if a%inicio==0:
            z+=inicio
        inicio+=1

    
    if not (a/2!=float) and(((a//1)==a) and ((a//a)==1)) or (a%2!=0) or (a==2):
        if (a!=1):
            return(z,True)
    if a==1:
        return(z,False)
    else:
        return(z,False)
           