def suma_divisores(n):
    x = []
    for m in range(1, n):
        if n % m == 0:
            if n == m:
                x.append(0)
            else:
                x.append(m)
    v = False
    print(sum(x)%a)
    if sum(x) >= 1:
        if sum(x) == 1:
            v = True
        elif sum(x) == 0:
            v = True


    return(sum(z), w)