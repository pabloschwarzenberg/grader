# completa el código de la función
def suma_divisores(a):
 divisor = float(input ("Ingrese el valor del divisor: "))
    
  for suma_divisores(divisor):
    if a % i == 0 :
  
 return sum(divisor)
           