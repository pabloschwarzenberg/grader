#Nota Final

#ENTRADA

X = float(input("valor de nota 1: "))

Y = float(input("valor de nota 2: "))

Z = float(input("valor de nota 3: "))

W = float(input("valor de nota 4: "))


#Procesamiento

final = 0.3 * X + 0.3 * Y + 0.3 * Z + 0.1 * W

 
#SALIDA

print("El valor final equivale a: ", round (final,1))
