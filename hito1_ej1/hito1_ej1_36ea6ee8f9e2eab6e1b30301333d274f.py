#Nota final

PT = float(input("TAREAS:"))

PI = float(input("INTERROGACIONES:"))

NE = float(input("EXAMEN:"))

PP = float(input("PRESENTACION:"))

LOOOL = 0.3 * PT

DAB = 0.3 * PI

SISI = 0.3 * NE

NOH = 0.1 * PP

FAINAL = LOOOL + DAB + SISI + NOH

Redondeo = round(FAINAL, 1)

print("Alumno GG, tu promedio final es", Redondeo,)