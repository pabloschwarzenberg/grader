#Nota final
      # entrada

pt = float(input("inserte nota tarea: "))
pi = float(input("inserte nota interrogaciones: "))
ne = float(input("inserte nota examen: "))
pp = float(input("inserte nota precentacion: "))

# procesamiento

resultado = 0.3*pt+0.3*pi+0.3*ne+0.1*pp

#salida

print(resultado)
